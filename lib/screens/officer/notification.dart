import 'package:flutter/material.dart';

class OfficerNotificationPage extends StatelessWidget {
  const OfficerNotificationPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Officer Notifications')),
      body: const Center(child: Text('Officer Notification Page')),
    );
  }
}
